{
  "name": "@neonjs/core",
  "version": "0.0.1",
  "description": "NeonJS core package",
  "author": "Matt Bise",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "watch": "yarn clean && tsc -p tsconfig.json --watch",
    "build": "yarn clean && tsc -p tsconfig.json",
    "test": "jest --watch",
    "clean": "rimraf ./dist"
  },
  "peerDependencies": {
    "inversify": "^5.0.1",
    "reflect-metadata": "^0.1.13"
  },
  "dependencies": {
    "class-validator": "^0.12.2"
  }
}
