{
  "name": "@neonjs/react",
  "version": "0.0.1",
  "description": "React bindings for NeonJS",
  "author": "Matt Bise",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "watch": "yarn clean && tsc -p tsconfig.json --watch",
    "build": "yarn clean && tsc -p tsconfig.json",
    "test": "jest --watch",
    "clean": "rimraf ./dist"
  },
  "peerDependencies": {
    "@neonjs/core": "0.0.1",
    "inversify": "^5.0.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@neonjs/core": "0.0.1"
  }
}